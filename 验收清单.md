# Codex + CC Switch + Clash + VS Code 最终验收清单

## 当前结论（2026-09-16）

**结论：自动化与当前运行态检查通过，最终整体验收暂定“有条件通过”。**

- 自动检查结果：`0 FAIL / 0 WARN`；Rust、前端、TypeScript 和启动器测试均通过，生产构建成功，已安装二进制与构建产物哈希一致。
- 说明：`acceptance-check.sh` 原先只认“官方托管占位形态”，当前卡片是第三方时会把正常的 `requires_openai_auth = true` 误判为 FAIL。该判定已改为按当前卡片类型分别校验，并新增“路由形态必须与当前卡片一致”“live 配置不得内嵌非占位符凭据”两条断言。
- 当前运行态：CC Switch `15721`、Clash `7897` 均可连接；连续 6 次真实代理请求全部成功，最慢 503 ms。
- 尚未执行会中断本次 Codex 会话的测试：真实 Provider 往返切换、跨 Provider 续聊、主动退出、异常恢复、冷重启/TUN 清理。
- 仍需时间验证：覆盖一次真实 token 到期的 24～48 小时运行，以及其他设备同时联网时的稳定性。
- 因此目前不能把全部 P0/P1 项标为最终通过；下方保留未勾选项作为后续人工执行记录，不能用静态检查代替真实请求。

### 2026-09-16 Clash 协议入口修复

- 发现 Clash Verge 使用的 Tauri deep-link 插件会在注册协议时，把 `clash-verge-handler.desktop` 的 `Exec` 强制重写为当前可执行文件 `/usr/bin/clash-verge`；因此此前的一次性手工修正会在后续启动或第二实例注册时失效。
- `clash-verge-guarded` 现会在启动 Clash 后短时轮询并自动修复 handler，同时重新绑定 `clash://` 和 `clash-verge://`，保留订阅链接功能且不允许协议入口绕过退出清理。
- 启动器测试新增“假 Clash 主动覆盖 handler”场景，确认守卫可以自动恢复；新守卫已安装，当前 handler 与两个 MIME 默认关联也已修复，未停止现有 Clash/CC Switch。
- 修复后于 `2026-09-16 10:58` 重新运行只读验收：`0 FAIL / 0 WARN`。

可重复执行的只读检查命令：

```bash
cd /path/to/cc-switch-stable-custom
./scripts/linux/acceptance-check.sh
```

该脚本不会切换 Provider、退出 CC Switch/Clash、改写配置或输出 token。

### 2026-09-16 实现可行性核验（回答“现在能不能达成清单”）

核验方式：重新运行只读脚本、重跑 Rust 全量测试、在源码中逐条定位实现与单测。结论分三类，只有 A 类可以由当前代码自动证明。

**A. 已实现，且有可复现的自动化/静态证据**

| 编号 | 清单项 | 实现位置与证据 |
| --- | --- | --- |
| A-1 | 2 模型目录解析；11 官方模型 / DeepSeek 两个模型 | `codex_config.rs` 目录构建 + 验收脚本 `codex debug models` 断言 |
| A-2 | 3 每个模型只暴露自己声明的推理档位 | `codex_supported_reasoning_levels`；`native_responses_catalog_honors_per_model_reasoning_levels`、`vendor_catalog_honors_per_model_reasoning_levels` |
| A-3 | 3 `xhigh` 按原值透传、不硬编码 | `transform::tests::test_output_config_xhigh_maps_verbatim`、`test_output_config_max_maps_to_reasoning_effort_xhigh` |
| A-4 | 5 历史统一到共享 `custom` 分组 | `codex_history_migration.rs`；脚本实测 99 活动 / 53 归档 / 340 线程全部为 `custom`，不可读 0 |
| A-5 | 7 托管凭据不落盘到 Provider 卡片 | 脚本断言 `token_snapshots=0`；`codex_config.rs` 占位符清洗 |
| A-6 | 7 `invalid_grant` / `refresh_token_reused` 才判失效 | `commands::codex_oauth::tests::only_explicit_refresh_token_failure_is_expired`、`bare_http_status_does_not_prove_refresh_token_invalid`、`refresh_error_code_accepts_openai_error_shapes` |
| A-7 | 7 托管失败不得静默回退，且官方凭据不得发往第三方 | `forwarder.rs::reject_proxy_placeholder_for_managed_account_upstream` + `codex_oauth_upstream_rejects_proxy_managed_placeholder_header`；`official_auth_fallback_for_third_party_detection`、`neutralize_proxy_oauth_fallback_flips_only_active_custom_true` |
| A-8 | 7 原生缓存与托管库不得互相覆盖 | `ensure_live_auth_guard_rejects_rotated_refresh_without_mutating_live_bundle`、`clear_live_auth_guard_rejects_rotated_refresh_without_mutating_live_bundle`、`codex_oauth_auth::tests::adopt_account_refresh_token_*`；脚本断言两份 refresh token 不同 |
| A-9 | 7 钥匙环竞争已被识别并按模式放行 | `codex_takeover_keeps_stored_flag_when_auth_store_is_keyring_backed`、`codex_config_auth_store_mode` |
| A-10 | 9/10 启动器：过期租约、PID 复用、重复启动、协议 handler 自愈、最后使用者退出、系统代理置 `none`、信号转发 | `scripts/linux/test-launchers.sh`（本机可重复执行） |
| A-11 | 13 原子写入与失败回滚 | `config::tests::atomic_write_replaces_existing_file`、`atomic_write_preserves_destination_when_windows_replace_fails`、`services::proxy::tests::codex_snapshot_rollback_*`、`rollback_failed_takeover_activation` |
| A-12 | 15 迁移幂等 | `codex_history_migration.rs` 测试内显式“第二次执行应当无事可做（幂等）”，`rerun == 0` |
| A-13 | 11 本机托管代理只监听回环、凭据 600 | 脚本断言 `127.0.0.1:15721`、两个凭据文件 `600` |

**B. 已实现，但只能用真机实测判定（本轮为避免切断会话未执行）**

- P0 1：真实 Provider 往返切换三次、切换中在途请求归属。
- P0 3：推理偏好在 VS Code 界面按卡片恢复、`xhigh` 切到不支持模型后的界面回退。
- P0 5/6：跨 Provider 历史在界面可见、跨模型继续旧历史且不产生副本。
- P0 9/10：主动退出 30 秒观察、异常恢复、`desired_state` 语义。
- P1 12/13：冷启动、注销、休眠唤醒、Wi-Fi/热点切换、100 次快速切换压测。
- P1 15：真实旧版本升级路径（而不是单元级迁移模拟）。
- P1 16：24～48 小时并有真实 token 到期。
- P0 8：其它设备同时联网时的稳定性。

**C. 当前尚未实现，需要新开发（对应清单项不能仅靠测试通过）**

| 编号 | 清单项 | 现状 |
| --- | --- | --- |
| C-1 | 17 统一健康面板（Provider + 认证所有者 + Clash 核心状态 + 最近刷新结果） | 只有 `ProxyStatus`（运行态、当前 Provider、最近请求/错误）与单卡片“连通性检查/健康失败计数”；没有 Clash 核心状态与认证所有者字段 |
| C-2 | 17 一键导出脱敏诊断包 | 已有脱敏工具与诊断日志开关（`redact_known_secrets`、`redactFrontendLogText`），但没有“打包导出日志+状态”的入口 |
| C-3 | 17 单入口区分 `15721` / `7897` / DNS / 上游超时 / OAuth 失效 | 提示分散在各处（重登录、超时、网络提示），没有统一分类入口 |
| C-4 | 10 连续失败“指数退避并最终停止” | 当前只有 `Restart=on-failure` + systemd 默认 `StartLimitBurst=5`（快速失败会停，但无退避曲线） |
| C-5 | 10 恢复期间提示“正在恢复” | 现有“恢复中…”字符串属于数据库/Skills 备份恢复，不是代理崩溃恢复提示 |

**核验结论**：P0/P1 的机制在代码层面已经具备（A 类），剩下的主要是必须真机跑一遍的 B 类；真正缺实现的是 P2 诊断能力（C-1～C-3）和 P1 #10 的退避/恢复提示（C-4、C-5）。因此“现在能不能达成清单”的准确回答是：**P0 机制齐备但未实测，P1 除 #10 的退避与提示外齐备，P2 尚未实现。**

## 本次执行记录

| 范围 | 状态 | 实际结果 |
| --- | --- | --- |
| 环境版本 | 已记录 | CC Switch `3.20.3`（基线提交 `bd247a4` 加本次未提交修复）；Codex CLI `0.154.0-alpha.6.2`；VS Code `1.136.1`；Clash Verge Rev `2.5.2`；Mihomo `1.19.29`；Ubuntu `22.04.5` / GNOME `42.9` / Linux `6.8.0-138` |
| Rust 回归测试 | 通过 | `2852 passed / 0 failed / 5 ignored`；一次并行全局设置测试抖动，单项复测及第二次全量均通过 |
| TypeScript / 前端 | 通过 | `pnpm typecheck` 通过；`137` 个测试文件、`1111` 项单元测试通过 |
| 启动器集成 | 通过 | 过期租约、PID 复用、重复启动、协议 handler 被 Clash 覆盖后的自愈、最后使用者退出、系统代理关闭和信号转发全部通过 |
| 生产构建与部署 | 通过 | `tauri build --no-bundle` 成功；安装文件与 release 二进制 SHA-256 均为 `7cae9b7b05e02490d2129ebadaa5446fc91e0528342dfbd944636108539097de` |
| 官方认证所有权 | 通过（静态/运行态） | 当前 Codex 仅走 `127.0.0.1:15721` 且只带 `PROXY_MANAGED` 占位符；2 个托管卡片均无持久化 token 快照。`requires_openai_auth` 取值随当前卡片类型而定：官方托管为 `false`，第三方接管的 `true` 由同表的占位符短路，不会把官方登录发往第三方（见本次核验 A-7） |
| 原生登录隔离 | 通过（静态/运行态） | `auth.json` 内残留的原生 refresh token 与 CC Switch 托管库均不相同，且当前路由不会读取它；两个凭据文件权限均为 `600` |
| 模型目录解析 | 通过（CLI） | 官方目录解析出 11 个模型；DeepSeek 解析出 `DeepSeek-Flash`、`DeepSeek-V4-Pro`，推理档位均仅为 `low/high/max` |
| 推理偏好 | 部分通过 | 当前官方卡片保存的选择为 `max`；跨 Provider、跨重启是否按卡片恢复仍待界面实测 |
| 历史统一 | 通过（数据层） | 99 个活动 JSONL、53 个归档 JSONL、SQLite 中 340 个线程全部归入 `custom`，不可读文件为 0 |
| 跨模型续聊 | 待实测 | 数据可见性已满足，但必须真实发送后续消息验证上下文与工具调用兼容性 |
| Clash 当前网络 | 通过（短时） | `7897` 监听正常；6/6 次外网代理请求成功，无 timeout |
| 主动退出/异常恢复 | 待实测 | 新启动器和 systemd 策略已安装，当前进程未重启；为避免切断本次会话，本轮未退出 CC Switch/Clash |
| 冷重启、TUN、休眠 | 待实测 | 必须在本次会话结束后现场执行 |
| token 到期/多设备 | 待观察 | 需连续 24～48 小时并覆盖一次真实 token 刷新 |
| 安装包签名 | 待发布配置 | `.deb` 已生成；updater 签名因本机没有发布私钥而未生成，不影响当前安装二进制运行 |

已知数据提示：10 个子代理 rollout 指向 2 个已被用户删除的父会话；父文件仍在回收站（约 354 MB）。这不是 Provider 分组丢失，本次未擅自恢复。

## 验收前提

- [x] 本方案采用“CC Switch 独占当前官方认证”：托管 OAuth 凭据只由 CC Switch 保存和刷新，Codex 当前请求只访问本地代理。
- [x] 记录 CC Switch 版本/提交、Codex CLI 与 VS Code 扩展版本、Clash 与 Mihomo 版本、操作系统版本。
- [x] 已有官方托管账号、DeepSeek 卡片和其他第三方 API 卡片；真实往返请求仍按 P0 第 1 项执行。
- [x] 对本次改动涉及的数据库、二进制、启动器和 systemd 配置建立备份；检查过程只比较凭据哈希，不输出明文。
- [ ] 每项记录测试步骤、实际结果、脱敏日志和截图。

## P0：核心功能

### 1. Provider 双向切换

- [ ] 执行“官方托管 → DeepSeek → 其他卡片 → 官方托管”，至少循环三次。
- [ ] 每次切换后在 VS Code Codex 中创建新对话并发送消息。
- [ ] 每次都能正常响应，无 401、登录失效、模型不可用或要求重新登录。
- [ ] 实际请求只发往当前 Provider，不混用上一个 Provider 的地址、模型或凭据。
- [ ] 切换后无需重启系统；CC Switch、VS Code 重启后当前 Provider 仍正确。
- [ ] 切换时正在运行的请求应正常完成或明确取消，不能串到新 Provider。

### 2. 模型目录

- [ ] VS Code 和 Codex CLI 均显示 CC Switch 配置的真实模型名，而非笼统的“自定义”。
- [ ] 模型显示名、模型 ID、顺序和当前选中项与 CC Switch 一致。
- [ ] 选择模型后，实际上游请求中的 `model` 与所选项一致。
- [ ] 切换 Provider 后模型目录及时刷新；若产品要求重载 VS Code，应有明确提示。
- [ ] 无效或不完整的模型目录应显示明确错误，不能静默回退到错误模型。
- [ ] 用户自定义的外部模型目录不得被 CC Switch 修改或删除。
- [x] CLI 已验证 DeepSeek 目录包含两个真实模型名；`additional_speed_tiers` 允许为空，它本身不会导致模型显示为“自定义”。

### 3. 推理等级与记忆

- [ ] 每个模型只显示其声明支持的推理等级，不得所有模型固定显示相同档位。
- [x] 当前 DeepSeek 目录只声明 `low/high/max`，不包含 `medium/xhigh`；CLI 已按该集合解析。
- [ ] DeepSeek 设置为最高支持档后，切走再切回应恢复该设置。
- [ ] 支持 `xhigh` 的模型设置为 `xhigh` 后，切到不支持它的模型应安全回退；切回后恢复 `xhigh`。
- [ ] 推理偏好至少按 Provider 卡片保存，切回卡片及重启后仍保留。若要求同一卡片内按每个模型分别记忆，需另行实现并单独验收；Codex 的 `config.toml` 只有一个当前推理字段。
- [ ] 实际发送的推理参数与界面选择一致，无效档位不得发给上游。
- [ ] 模型能力依据具体模型和当前配置，不对所有 GPT 或 DeepSeek 模型硬编码相同档位。

### 4. Codex 完整代理能力

- [ ] 普通问答、流式输出和停止生成正常。
- [ ] 能读取工作区文件、执行终端命令、修改和创建文件。
- [ ] 连续多轮工具调用能够正确完成，调用 ID 不损坏。
- [ ] 在跨 Provider 历史中继续执行工具时上下文正确。
- [ ] 图片、搜索、长上下文等功能只在模型声明支持时启用。
- [ ] 不支持的能力应明确报错，不能伪装成功或静默换模型。

### 5. 历史记录跨 Provider 可见

- [ ] 官方和所有第三方 Provider 创建的普通历史均同时可见。
- [ ] 归档会话切换 Provider 后仍可见、可恢复。
- [ ] 历史标题、时间、排序、会话 ID 和归档状态保持不变。
- [ ] 多次切换不会生成重复历史或把同一会话拆成多个副本。
- [ ] 重启 CC Switch、VS Code 和电脑后历史仍完整。
- [x] 当前 99 个活动会话、53 个归档会话和 340 个 SQLite 线程均在共享 `custom` 分组，无旧分组和不可读文件。

### 6. 跨模型继续旧历史

- [ ] 在官方模型创建包含多轮工具调用的对话，再切到 DeepSeek 继续。
- [ ] 在第三方模型创建对话，再切回官方模型继续。
- [ ] 模型能够引用切换前的用户消息、回复和关键工具结果。
- [ ] 允许 Codex 正常压缩超长上下文，但不得因 Provider 切换丢失记录。
- [ ] 续聊失败时原会话文件不得被破坏或覆盖。
- [ ] 再次打开同一历史时仍能继续，不产生重复会话。

### 7. CC Switch 独占官方认证

- [ ] 真实 access token 和 refresh token 只由 CC Switch 账户库管理。
- [x] 托管模式使用 `127.0.0.1:15721`、`PROXY_MANAGED` 和 `requires_openai_auth=false`。
- [x] CC Switch 管理的 refresh token 未复制到 Codex 的 `auth.json`；托管 Provider 数据库记录也不再保存 token 快照。
- [x] 原生 Codex 登录缓存处于休眠/隔离状态，与托管 token 不同且不参与当前请求。
- [ ] 网络日志中不存在 Codex 进程直接发起的 OAuth 刷新。
- [ ] 多个 VS Code 窗口和 CLI 同时运行时，每个账号最多只有一个合并后的刷新请求。
- [ ] 等待超过 access token 有效期后仍可使用，不出现每天固定时间掉线。
- [ ] `codex login/logout` 即使改写休眠的原生缓存，也不得改变 CC Switch 托管账号、托管库或当前代理路由。
- [ ] `invalid_grant`、token reused 等永久错误应停止重试并明确要求重新登录。
- [ ] 托管失败时不得静默回退到 Codex 原生认证。
- [ ] 退出托管前必须停止 CC Switch 刷新任务，再进行明确的认证所有权交接。
- [ ] 检查范围包括 `auth.json` 和系统钥匙环，确保 Codex 原生续期机制未与 CC Switch 同时生效。

### 8. Clash 正常网络行为

- [ ] Clash 正常运行时，本机代理连续访问稳定，无间歇性全 timeout。
- [ ] 同配置的其他设备同时使用时，本机端口、DNS 和 TUN 状态保持正常。
- [ ] `7897` 监听进程与实际 Mihomo 核心一致，无端口抢占。
- [ ] Clash 节点切换后现有和新请求能在合理时间内恢复。
- [ ] DNS 请求不泄漏到错误接口，也不因 TUN 状态变化完全失效。
- [ ] CC Switch 能区分 Clash 不可用和上游 Provider 不可用。
- [x] 当前短时抽样 6/6 次真实代理请求成功，耗时 168～503 ms；这不能替代多设备和长时间测试。

### 9. Clash 主动退出清理

- [x] 桌面、开机自启动、`clash://` 与 `clash-verge://` 当前均进入受保护启动器；Clash 再次生成原版 handler 后由守卫自动恢复，隔离回归测试已通过。
- [ ] 点击退出 Clash 后，Clash UI、Mihomo、`7897`、系统代理和 TUN 按设计停止。
- [ ] 按本机选定策略，最后一个 Clash UI 退出后系统代理必须明确变为 `none`；不恢复退出前的手工代理，确保不会继续妨碍直连。
- [ ] 路由、DNS 劫持、虚拟网卡和防火墙规则无残留。
- [ ] 直连测试清除 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 并强制绕过代理。
- [ ] CC Switch 可以继续显示，但不得让系统流量指向已经死亡的 `7897`。
- [ ] 如果外部服务必须使用 Clash，CC Switch 应明确提示“Clash 已退出”，不能误报认证失败。
- [ ] 重启电脑且不启动 Clash 时，基础直连网络正常。

### 10. 主动退出与异常重连

- [ ] 用户点击退出 CC Switch 后观察至少 30 秒，进程和 `15721` 不得自动重现。
- [ ] 用户点击退出 Clash 后观察至少 30 秒，Mihomo 和 `7897` 不得自动重现。
- [ ] 主动退出记录为 `desired_state=stopped`，守护程序不得当成崩溃。
- [ ] 异常崩溃允许自动恢复，建议 10～15 秒内完成。
- [ ] 自动恢复后，新 PID、监听端口和网络状态稳定至少 30 秒。
- [ ] 连续失败应指数退避并最终停止，不能无限重启。
- [ ] 恢复期间请求提示“正在恢复”，不能修改登录状态或判定账号失效。
- [ ] 用户手动启动的 Clash 不归 CC Switch 所有；退出 CC Switch 不得关闭它。
- [ ] CC Switch 自动启动的 Clash 只有在最后一个使用者退出后才能停止。
- [ ] 多个 Codex/VS Code 进程使用有效 PID 或租约计数，不能仅判断标记文件是否存在。
- [ ] CC Switch 异常退出留下的过期 PID 标记自动清理。
- [ ] 完全退出清理测试先关闭自启动和守护；异常恢复测试保持它们开启。
- [ ] 每次判定前连续检查三次、间隔两秒；若期间重连，重新建立测试基线。

主动退出和异常恢复必须分成两个用例，不能在同一轮里混判：

1. 先记录 UI、核心、服务 PID、端口和系统代理作为基线。
2. 主动退出只点击一次正常“退出”，随后在 0、2、4、6 秒及此后每 2 秒检查到 30 秒；任何进程或端口重现均判失败。
3. 异常恢复另行通过可控崩溃执行；恢复期间出现短暂重连是预期，但必须在 10～15 秒内稳定，并连续三次检查一致后才通过。
4. 若测试动作会切断正在使用的 Codex 会话，先保存测试会话 ID，在第二终端记录结果；不得把连接中断本身误判成认证掉线。

### 11. 安全隔离

- [ ] access token、refresh token、API Key 和授权头不出现在普通日志、弹窗或诊断包。
- [ ] 含凭据的文件仅允许当前用户读取。
- [ ] CC Switch 管理代理只监听 `127.0.0.1`，不得暴露到局域网。
- [ ] 外部设备无法使用 `PROXY_MANAGED` 调用本机代理。
- [ ] 官方凭据不得发往第三方 Provider，第三方 Key 不得发往 OpenAI。
- [ ] 备份、数据库导出和崩溃日志同样执行凭据脱敏。
- [x] 当前 CC Switch 代理监听地址为 `127.0.0.1:15721`，凭据文件权限为 `600`，自动检查不输出凭据。

## P1：稳定性与恢复

### 12. 启动顺序、重启和休眠

- [ ] 分别测试先启动 VS Code、CC Switch、Clash 的所有常见顺序。
- [ ] Clash 核心未就绪时，CC Switch 应等待、自动启动或明确失败，不能假装可用。
- [ ] 冷启动、注销、重启后的自动启动状态一致。
- [ ] 休眠唤醒后 Provider、认证、代理和历史均恢复正常。
- [ ] Wi-Fi、有线网络和热点切换后无需手工重启。
- [ ] 单独重启任意一个组件不会破坏其他组件的持久配置。

### 13. 原子写入与失败回滚

- [ ] 快速切换 Provider 至少 100 次，并在切换中反复启动或重载 VS Code。
- [ ] Codex 只能看到完整旧状态或完整新状态，不能看到半写入配置。
- [ ] `auth.json`、`config.toml` 和模型目录始终为合法格式。
- [ ] 无效 Provider、代理启动失败或模拟写盘失败时，保留上一个可用配置。
- [ ] 恢复失败不得覆盖用户在此期间新产生的登录状态。
- [ ] 关闭托管后不得残留 `15721`、`PROXY_MANAGED`、托管 Provider 表或所有权标记。
- [ ] 备份缺失或损坏时，兜底清理仍能恢复为可启动状态。

### 14. 配置持久性

- [ ] 通过 CC Switch 编辑的模型目录和推理设置在切换、重启后保持。
- [ ] `cc-switch-model-catalog.json` 能从数据库确定性重建。
- [ ] CC Switch 生成文件允许被重建覆盖，但界面应明确说明。
- [ ] 用户自己的外部模型目录不得被修改、删除或替换。
- [ ] 无关 TOML 配置、MCP 配置和注释尽可能保留。
- [ ] 删除当前 Provider 后能恢复到有效备份或安全默认状态。

### 15. 升级与迁移

- [ ] 从含旧 Provider ID 的版本升级成功。
- [ ] 旧普通会话、归档会话和 SQLite 索引全部迁移到共享分组。
- [ ] 重复执行迁移不会重复修改、生成副本或再次分裂历史。
- [ ] 升级后模型目录、推理偏好和认证所有权正确。
- [ ] 迁移中断后再次启动可以继续或安全回滚。
- [ ] 安装程序与通过测试的源码构建产物哈希一致。
- [x] 当前安装二进制与 production release 构建产物哈希一致。

### 16. 长时间运行

- [ ] 连续运行至少 24～48 小时，并覆盖一次真实 token 到期。
- [ ] 定时执行消息和工具请求，不出现固定时间掉线。
- [ ] 临时断网、Clash 换节点和核心重启后能够恢复。
- [ ] 无持续增长的僵尸进程、内存、文件描述符或临时文件。
- [ ] CC Switch、Clash 和 Codex 不产生端口抢占或重启风暴。
- [ ] 每次失败均能在日志中找到准确时间、组件和原因。

## P2：诊断与发布

### 17. 故障诊断

- [ ] 能区分 `15721` 未启动、`7897` 未启动、DNS、上游超时和 OAuth 失效。
- [ ] CC Switch 显示当前 Provider、认证所有者、Clash 核心状态及最近刷新结果。
- [ ] 错误提示包含可执行的恢复建议，不能统一显示“无法连接”。
- [ ] 可导出脱敏诊断信息，且不包含完整凭据。

### 18. 发布门槛

- [ ] P0 必须全部通过；历史丢失、凭据混用或系统断网一律阻止发布。
- [ ] P1 在日常使用版本发布前全部通过。
- [ ] TypeScript、前端测试和 Rust 测试全部通过，不保留失败测试。
- [ ] 使用生产模式构建，并在全新用户目录执行冒烟测试。
- [ ] 至少完成一次重启、一次主动退出、一次异常崩溃和一次 token 到期测试。
- [ ] 每个失败项记录复现步骤、日志和修复版本，修复后重新执行相关回归测试。

## 本次备份与产物

- 数据库备份：`~/.cc-switch/backups/cc-switch.db.pre-managed-auth-scrub-20260915-acceptance`
- 旧应用二进制：`~/.cc-switch/backups/cc-switch-fixed-3.20.3.pre-acceptance-final-20260915`
- 当前应用二进制：`~/.local/bin/cc-switch-fixed-3.20.3`
- Debian 包：`src-tauri/target/release/bundle/deb/CC Switch_3.20.3_amd64.deb`
- 启动器源码与测试：`scripts/linux/`
- 协议自愈前守卫备份：`/usr/local/bin/clash-verge-guarded.before-protocol-self-heal-20260916`

当前正在运行的 CC Switch/Clash 进程没有被强制重启；当前 CC Switch PID 仍持有替换前的旧 inode。下一次正常退出并重新启动后才会完整加载新二进制、新启动器和新的退出清理逻辑。
