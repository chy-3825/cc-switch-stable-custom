# CC Switch Stable Custom 工作清单

## 已完成

- [x] 基于上游 CC Switch `3.20.3` 建立稳定分支并保留 MIT License。
- [x] 实现 CC Switch 托管 Codex OAuth，Codex 仅访问本地管理代理。
- [x] 隔离 Codex 原生登录缓存，避免双方并发刷新和互相踢下线。
- [x] 统一官方与第三方 Provider 的 Codex 历史分组。
- [x] 迁移活动、归档 JSONL 与 SQLite 线程索引，并保证迁移幂等。
- [x] 生成 Provider 模型目录，支持真实模型名与模型能力对应的推理等级。
- [x] 保存 Provider 级模型和推理偏好，并在不支持的档位上安全回退。
- [x] 加固代理转发、OAuth 刷新合并、永久认证错误处理与日志脱敏。
- [x] 实现 Linux CC Switch 启动器和 Clash Verge 生命周期守卫。
- [x] 增加启动器隔离测试与只读验收脚本。
- [x] 完成 Rust、TypeScript、前端、启动器和 production build 自动测试。

## 发布前必须完成

- [ ] 实测“官方托管 → DeepSeek → 其他卡片 → 官方托管”三轮往返。
- [ ] 在 VS Code 与 CLI 验证模型目录、实际请求模型和推理参数一致。
- [ ] 实测跨 Provider 打开并继续包含工具调用的历史会话。
- [ ] 实测正常退出 CC Switch/Clash 后 30 秒内不被错误拉起。
- [ ] 实测可控异常退出后的自动恢复和退避行为。
- [ ] 完成冷重启、休眠唤醒、TUN 清理及网络切换测试。
- [ ] 连续运行 24～48 小时并覆盖一次真实 token 到期。
- [ ] 在其他设备同时使用网络时进行长时间稳定性测试。
- [ ] 在干净用户目录完成安装和首次启动冒烟测试。

完整判定方法和记录见 [验收清单](验收清单.md)。

## 后续维护策略

- 当前版本以稳定为主，默认冻结，不追随上游自动更新。
- 只有安全问题、上游协议变化、Codex 配置格式变化或明确需要的新功能才升级。
- 每次升级从当前稳定标签创建迁移分支，先备份数据库和配置。
- 合并上游后必须重新运行全部自动测试及受影响的 P0/P1 验收。
- 未通过验收的构建不得覆盖当前已安装稳定二进制。
- 禁止把 `~/.cc-switch`、`~/.codex`、Clash 订阅、token、API Key 或诊断原始日志提交到仓库。
